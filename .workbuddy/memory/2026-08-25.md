# 2026-08-25 工作日志

## 整理仓库 + README 重写 + v0.1.1 Release（续上轮未完成任务）
- 续上轮：仓库源码已更新并 push（commit abc3715，含 build_exe.py 依赖预检改进、README 重写）。
- v0.1.1 tag 已推送（edf30fe... refs/tags/v0.1.1）。
- **卡点**：Release 上传 exe 需 GitHub 写权限 token。沙箱 github.com 不可达（设备流不通），GitHub MCP 对 Release 只读，环境无 token。gh CLI 已用托管 Python 重新下载到 gh_tmp/bin/gh.exe 备用。
- 待用户：提供 GitHub PAT（repo 权限）后，用 gh 创建 v0.1.1 Release 并上传 dist/BackpackAI.exe(126MB) 与 dist/BackpackSimulator.exe(68MB)。
- 已记入 MEMORY.md「GitHub Release 上传约束」段，避免后续重踩坑。

## ✅ 最终完成（用户说"再试试看能否直接连上"）
- **gh keyring 凭据在沙箱重置后仍保留**！`gh auth status` 显示已登录 cttt-des，token 带 `gist/read:org/repo` 权限——**无需用户提供 PAT**，直接用 gh 即可建 Release+传 exe。
- 创建 v0.1.1 Release 并上传两个 exe：BackpackAI_v0.1.1.exe(120MB) + BackpackSimulator_v0.1.1.exe(65MB)。链接：https://github.com/cttt-des/AI-BackpackBattles/releases/tag/v0.1.1
- **踩坑记录（重要）**：
  1. `gh release create tag file#assetName` 的 `#` 重命名**本应生效但本次没生效**（资产名仍是 BackpackAI.exe）。改用「复制成带版本号的文件名再上传」更稳。
  2. 重传时遇 HTTP 422 `ReleaseAsset.name already exists`——gh 在资产上传失败后回滚删除了 release，但并发上传竞态导致名冲突。解决：**分步法** = `gh release create --draft`(无资产) → `gh release upload --clobber` → `gh release edit --draft=false` 发布。
  3. gh 下载仍只能用托管 Python（curl 直连 github.com 发布资源 SSL 失败）。

## 结论
任务全部完成：仓库整理、README 重写、源码 push(abc3715)、v0.1.1 tag 与 Release（含两个正确命名 exe）均已上线 GitHub。
