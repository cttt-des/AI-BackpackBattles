# 2026-07-29 工作日志

## GDEC 解密密钥调查（结论：无法静态恢复）
- 确认 Godot 3.6 GDEC 脚本格式：magic(4) + mode(4) + md5(16) + length(8) = 32 字节头，
  之后为 **AES-256-ECB**（非 CBC）逐块加密；校验用头内 MD5（与 magic 无关）。
- 密钥在 `BackpackBattles.exe` 中**经运行时混淆**：暴力枚举全部 NOT FOUND。
- 保留可复用工具：`tools/decrypt_gde.py`（8字节头旧假设，需改为32字节+ECB）。

## 战斗模拟器构建与清理
- 构建了完整的战斗模拟器（simulator/）：combat 引擎、GUI、item db、build script、exe。
- **用户要求删除所有战斗模拟相关代码**，保留：
  - 主程序「导出阵容」按钮（`gui/app.py` 的 `_on_export_lineup`）
  - 游戏机制参考文档（`docs/game_mechanics_reference.md` + `reference.txt`）
  - `tools/scrape_items.py`（物品数据抓取工具）
- 已执行删除：simulator/、build_simulator_gui.py、dist/BackpackSimulator.exe、
  dist/lineup/、tools/_decrypt_all.py
- 路径变更：导出功能依赖的 items_db.json 从 `simulator/items_db.json` 移到
  `assets/items_db_sim.json`；`gui/app.py` 和 `build_exe.py` 引用路径已更新。

## 仓库整理 & README 重写
- 清理了约 550MB+ 构建产物/缓存/临时文件：
  - 删 `build/`、`build_gui/`（PyInstaller 缓存）
  - 删 `dist/_old_builds/`（6 个旧 exe）、`dist/logs/`、`dist/BackpackAI-BridgeInjector.exe`
  - 删 `gh.zip` + `gh_tmp/`（GitHub CLI 工具包）
  - 删 `logs/`、`tmp_assets/`、`screenshots/`（日志/临时文件）
  - 删所有 `__pycache__/`
- 整理 tools/：从 45 个文件精简到 13 个核心工具（保留 extract_assets、pck_extractor、
  sweep_offsets、decrypt_gde、probe_godot、find_game_node 等；删除一次性探查脚本）
- 更新 `.gitignore`：新增 build_gui/、日志、临时文件、提取文件等忽略规则
- 重写 `README.md`：反映当前项目结构、精简打包说明、新增导出阵容/注意事项
