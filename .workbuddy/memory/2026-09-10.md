# 2026-09-10 工作日志

## 整理仓库 + 发 v0.1.2 模拟器 Release
- 清理：`.zcode/plans/plan-sess_*.md` 被错误跟踪（.zcode/ 已在 .gitignore），`git rm --cached` 取消跟踪。工作树其余干净。
- README 下载表对账：原写 v0.2.0（远程从未发布，属虚构）→ 改为模拟器 **v0.1.2** + AI **v0.1.1**（均为真实已发布版本）。
- 提交 `f725e7b` 并 `git push -u origin master`（远程从 c3d89f2 快进到 f725e7b，重建 upstream）。
- 打标签 `v0.1.2`（指向 f725e7b）并推送到远程。
- 发 Release：分步法 `release create --draft` → `release upload --clobber gh_tmp/BackpackSimulator_v0.1.2.exe`（67MB）→ `release edit --draft=false`。下载链接 HTTP 200 验证通过。
- gh 不在 PATH；从工作区 `gh.zip` 解压 `gh_tmp/bin/gh.exe` 使用（gh_tmp 已在 .gitignore）。
- 待确认：用户明确要 v0.1.2（非 README 原写的 v0.2.0）；v0.2.0 的 exe 构建仍躺在 dist/ 但未作为 Release 发布。
